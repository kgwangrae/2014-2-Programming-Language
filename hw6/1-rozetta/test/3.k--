(* fibonacci *)

let x := 0 in
let y := 1 in
let z := 1 in
let k := 0 in
let c := 0 in

read k;
while (c = 0) do (
	if (z < k) then (
		write z;
		x := y;
		y := z;
		z := x + y
	) else (
		c := 1
	)
)
